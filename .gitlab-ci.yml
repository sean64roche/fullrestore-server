stages:
  - test
  - build
  - publish

include:
  - local: packages/.gitlab-ci.yml

publish-packages:
  stage: publish
  script:
    - echo "Coordinating package publishing"
  only:
    - tags

build-containers:
  stage: build
  script:
    - echo "Coordinating api container building"
  only:
    - tags